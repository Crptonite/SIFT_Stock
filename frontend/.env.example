# Backend API URL — change to your Vercel deployment URL in production
VITE_API_URL=http://localhost:3001

# Neon Auth base URL (from Neon Console → Auth → Settings)
# Leave blank to use email/password only; OAuth buttons will show a setup reminder.
VITE_NEON_AUTH_URL=https://ep-gentle-breeze-adx0k43v.neonauth.c-2.us-east-1.aws.neon.tech/neondb/auth

# Admin token — must match ADMIN_TOKEN in backend .env
VITE_ADMIN_TOKEN=sift-admin-dev
